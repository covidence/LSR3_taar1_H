{
  "build": { "dockerfile": "./Dockerfile" },

  "customizations": {
    "vscode": {
      "extensions": [
        "REditorSupport.r",
        "ms-vscode.live-server"
      ]
    }
  },

  "postCreateCommand": "R -e 'renv::restore()'"
}
